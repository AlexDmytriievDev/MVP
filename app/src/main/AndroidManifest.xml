<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.gitmvpapp">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="com.example.gitmvpapp.app.MvpApplication"
        android:allowBackup="true"
        android:icon="${appIcon}"
        android:label="${appName}"
        android:supportsRtl="true"
        tools:ignore="LockedOrientationActivity"
        tools:replace="android:label">

        <activity
            android:name=".ui.flow.splash.view.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/ThemeFullScreen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.flow.authorization.login.view.LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/ThemeTransparentStatusBar" />
    </application>

</manifest>